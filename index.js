// Code your solutions in this file
function writeCards(name, event){
    for(let i=name; i < name.length; i++){
        const name = name[i];
        
        return `Thank you, ${name}, for the wonderful ${event} gift!`
    }
}

function countDown(number) {
    for (let i = number; i >= 0; i--) {
      console.log(i);
    }
  }